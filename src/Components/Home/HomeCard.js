import React from 'react';
import {
    Text, View,
    ScrollView,
    Image, TextInput,
} from 'react-native';

export const HomeCard = () => {
    return (
        <ScrollView>
            <View >
                <Text>
                    HomeCard__Welcome
                </Text>
            </View>
        </ScrollView>   
    )
}